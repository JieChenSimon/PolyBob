# PB-0040 组合策略组件登记（2026-08-25）

本登记用于防止“单项未达最终目标就丢弃”导致的研究偏差。所有组件均来自真实 OKX BTC-USDT 历史 L2 采样与真实 `SimulationService` 回放；它们不是盈利承诺，也不能单独进入真实交易。

| component_id | 机制 | 22 日成本后收益 | 胜率 | 组件用途 | 状态 |
|---|---|---:|---:|---|---|
| `okx.depth.follow.v1` | top-20 深度不平衡追随 | -0.4855% | 16.85% | 方向基线/高换手风险对照 | `CANDIDATE_COMPONENT` |
| `okx.depth.contrarian.v1` | top-20 深度不平衡反向 | -0.4174% | 17.58% | 反向方向与冲击成本改善 | `CANDIDATE_COMPONENT` |
| `okx.momentum.15m.v1` | 4×15 分钟 mid 动量确认 | -0.1622% | 20.25% | 趋势确认/减少不必要换手 | `CANDIDATE_COMPONENT` |

## 冻结组合结果

`okx.depth.contrarian.v1` + `okx.momentum.15m.v1` 在相同 22 日数据上完成一次组合回放：40 笔成交、20 笔闭合，成本后收益 `-0.03145%`，胜率 `10.00%`，最大回撤 `0.03283%`，显式成本 `2.6917`。它相较两个单项都减少了换手、成本和绝对亏损，但没有改善胜率，也不能 Promotion；组合状态为 `NOT_PROMOTABLE_AS_STANDALONE`/`diagnostic_not_promotion`，保留为下一轮训练区的组合输入，并记录“低换手/成本改善但信号精度不足”的失败边界。

## 组合预注册

- 组合：`okx.depth.contrarian.v1` + `okx.momentum.15m.v1`。
- 观测间隔：15 分钟；深度入场/退出：0.75/0.25；动量入场/退出：50/10 bps。
- 规则：两个组件方向一致时采用该方向，否则目标仓位为 0；仓位上限 1%，不使用验证结果反选权重。
- 组件权重：方向门控各 0.5；实现为一致性门控而非杠杆叠加。
- 组合仅用于研究回放；22 日样本、采样盘口、部分成交/容量证据不足时保持 `UNKNOWN` 或 `diagnostic_not_promotion`。

## 解释与晋级原则

组件单独失败只表示不能单独晋级；它仍可因降低成本、降低回撤、提高信号质量或提供互补信息而保留。组合若失败，记录失败归因（方向冲突、成本、换手、容量或数据覆盖），再在训练区预注册下一轮；不得在 OOS 上反复试到满意为止。
