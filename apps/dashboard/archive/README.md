# Archived dashboard components

This directory contains components removed from the production navigation.
They are retained only for historical reference and are not part of the
supported dashboard surface. `MarketDetailCharts.tsx` remains active through
the dynamic import in `MarketDetail.tsx` and must not be archived.
